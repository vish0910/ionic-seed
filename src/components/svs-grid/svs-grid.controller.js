(function (angular, _) {
    'use strict';

    angular
        .module('svs.svsGrid')
        .controller('svsGridController', svsGridController);

    function svsGridController() {

    }
}(angular, _));
